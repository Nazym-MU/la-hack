# super-three@0.181 vs @sparkjsdev/spark's peer (three@^0.180) is a known,
# intentional mismatch in this template. Pin the resolution so installs are
# reproducible for everyone instead of needing a manual --legacy-peer-deps flag.
legacy-peer-deps=true
